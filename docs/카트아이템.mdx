import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/blocks';
import CartItem from './CartItem';
import * as CartItemStories from './CartItem.stories';

<Meta title="Components/CartItem/Docs" component={CartItem} />

# CartItem 컴포넌트

<Description>
`CartItem`은 장바구니 페이지에서 개별 상품 정보를 표시하고 수량 조절, 삭제 기능을 제공하는 컴포넌트입니다.
</Description>

---

## 기본 예시

<Canvas>
  <Story name="Default" args={CartItemStories.Default.args}>
    {() => <CartItem {...CartItemStories.Default.args} />}
  </Story>
</Canvas>

---

## Props

<ArgsTable of={CartItem} />

---

## 다른 상태 예시

### 수량이 3개인 경우
<Canvas>
  <Story name="Qty3" args={CartItemStories.Qty3.args}>
    {() => <CartItem {...CartItemStories.Qty3.args} />}
  </Story>
</Canvas>

---

## 사용 방법

```jsx
import CartItem from './CartItem';
import shoes1 from '../assets/shoes_01.png';

<CartItem
  image={shoes1}
  brand="브랜드A"
  price={35000}
  qty={1}
  onInc={() => console.log('증가')}
  onDec={() => console.log('감소')}
  onRemove={() => console.log('삭제')}
/>;